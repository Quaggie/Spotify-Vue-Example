import * as types from '../mutation-types'

// initial state
const state = {
  id: 0,
  name: '',
  duration: 0,
  rating: '',
  artist: ''
};

// mutations
const mutations = {

};

export default {
  state,
  mutations
};
